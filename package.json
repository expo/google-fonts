{
  "name": "@expo/google-fonts",
  "version": "0.2.3",
  "private": true,
  "author": "Expo",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspace generator generate",
    "build-packages": "yarn workspace generator generate-packages;",
    "build-images": "yarn workspace generator generate-images",
    "publish-font-packages": "yarn workspace generator publish-font-packages",
    "generate-root-readme": "yarn workspace generator generate-root-readme",
    "update-directory-data": "yarn workspace generator update-directory-data",
    "lint": "eslint ."
  },
  "workspaces": [
    "packages/*",
    "font-packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-universe": "^14.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
