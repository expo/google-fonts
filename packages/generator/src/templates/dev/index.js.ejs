// This file is generated by @expo-google-fonts/generator
// If you want to modify it, go to packages/generator/templates

export { useFonts } from './useFonts';
<%-
variants
  .map(({ name, url }) => `export const ${name} = '${url}';`)
  .join('\n')
%>

<%-
deprecatedVariants
  .map(({ name, url }) => `/**
  * @deprecated This font has been removed from Google Fonts.
  */
export const ${name} = '${url}';`)
  .join('\n')
%>
